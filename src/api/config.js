// Configuración central para la API
export const API_BASE_URL = 'http://localhost:8080/api';

// Configuración de opciones de fetch
export const defaultOptions = {
    headers: {
        'Content-Type': 'application/json',
    }
};